name: ottis_pet
description: Ottis Pet é um aplicativo de adoção de animais

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  get: ^4.1.4
  splashscreen: ^1.3.5
  google_fonts: ^2.0.0
  introduction_screen: ^2.0.0
  intl: ^0.17.0
  flutter_svg: ^0.19.3
  font_awesome_flutter: ^9.0.0
  string_validator: ^0.3.0
  get_storage: ^2.0.2
  cloud_firestore: ^1.0.7
  google_sign_in: ^5.0.2
  firebase_storage: ^8.0.5
  firebase_auth: ^1.1.2

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/